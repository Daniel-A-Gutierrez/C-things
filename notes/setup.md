# Setup
install clangd extension and server for C lsp
install gcc/clang 
compile with `clang -o output input.c`;
get a nice compile_flags.txt off the internet so the lsp and compiler give me more errors
